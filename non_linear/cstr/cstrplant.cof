;NeuCon combined function 1.0
[CombinedFunction main]
CustomFunction cstr

[CustomFunction cstr]
;           .so/.dll depending the OS
file    cstr
options cstr.par cstr_out.dat
;Dummy initial (using defaults)
initial
